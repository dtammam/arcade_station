{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Activate .venv",
      "type": "shell",
      "command": "${workspaceFolder}/.venv/bin/activate", // For Linux/Mac
      "windows": {
        "command": "${workspaceFolder}\\.venv\\Scripts\\activate" // For Windows
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared"
      },
      "detail": "Activates the project's virtual environment"
    }
  ]
}