[pytest]
# PLAN.MD deliberately avoids pyproject.toml, so pytest is configured here.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    characterization: pins existing behavior as-is, bugs included, to protect refactors
